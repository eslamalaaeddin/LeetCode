package Arrays;

public class ArrayUtils {
    public static void printArray(int[] array) {
        for (int i = 0; i < array.length; i++) {
            System.out.println("Array contains " + array[i] + " at index: " + i);
        }
    }
}
